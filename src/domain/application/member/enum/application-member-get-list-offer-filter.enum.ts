export enum ApplicationMemberGetListOfferFilter {
    ACCEPTED = 'ACCEPTED',
    REJECTED = 'REJECTED',
    DEADLINE = 'DEADLINE',
    TEAM_LEADER_WAITING = 'TEAM_LEADER_WAITING',
    WAITING = 'WAITING',
}
